/*
ECMAScript 2016 introduced Array.includes() to arrays.
This allows us to check if an element is present in an array
(including NaN, unlike indexOf).
*/
const arr=[1,2,3,4,5,6,7,8,9,0]
const data=arr.includes(6)
console.log(data)

// spread...
